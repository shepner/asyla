# Cloudflare API token for Let's Encrypt DNS-01 (internal-proxy only).
# Create at https://dash.cloudflare.com/profile/api-tokens
# Permissions: Zone → Zone:Read, Zone → DNS:Edit (scope: asyla.org or "All zones").
# Copy to .env: cp .env.example .env && edit .env

CF_API_TOKEN=
