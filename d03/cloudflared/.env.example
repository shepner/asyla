# Cloudflare Tunnel Configuration
# Copy this file to .env and fill in your tunnel token
# DO NOT commit .env to git - it contains secrets

# Cloudflare Tunnel Token
# Get this from: Cloudflare Zero Trust → Networks → Tunnels → Create tunnel → Cloudflared
# Or from existing tunnel: Copy installation command, extract token
TUNNEL_TOKEN=your-tunnel-token-here
